<h1>声音列表</h1>

<table>
  <tr>
    <th>用户</th>
    <th>声音</th>
    <th></th>
  </tr>

<% @recs.each do |rec| %>
  <tr>
    <td><%= rec.uid %></td>
    <td><a href="<%= rec.rec_url %>">点我</a></td>
	<td><%= link_to '删除', rec, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />
